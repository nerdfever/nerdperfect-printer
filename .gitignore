# Test inputs and outputs saved during development — not part of the
# extension. (Saved pages and printouts can contain personal content —
# the repo is public, so they must never be committed.)
tests/
*.pdf
*.mhtml

# Web Store upload packages — built from the tracked sources.
*.zip

Improve_these/
screenshot.png
